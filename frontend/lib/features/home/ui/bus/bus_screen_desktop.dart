import 'package:flutter/material.dart';

class BusScreenDesktop extends StatelessWidget {
  const BusScreenDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
